/*
        void traverse() { traverse(head->r); }
        void traverse(struct node *t) {
            if (t != z) {
                traverse(t->l);
                cout << t->key << endl;
                traverse(t->r);
            }
        }
        */